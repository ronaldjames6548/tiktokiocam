
---
import Link from "@components/ui/link.astro";
import { localizePath } from "astro-i18next";
// @ts-ignore
import { Icon } from "astro-icon/components";
import { t } from "i18next";

const others = [
	
];
---
<div class="mx-auto px-4 py-8 flex flex-col md:flex-row">

    <div class="max-w-7xl mx-auto">
		<h2 class="mt-6 text-2xl font-semibold tracking-tight text-gray-900 dark:text-white sm:text-2xl">{t("features.title_photo_down")}</h2>
		 <p class="text-base leading-8 my-5">
                   {t("features.tiktok_photo_text1")} <a href="/download-tiktok-photo" class="group">{t("features.title_photo_downloader")}</a> {t("features.tiktok_photo_text2")} <a href="/download-tiktok-photo" class="group">{t("features.title_photo_downloader")}</a>
                </p>
		<h2 class="mt-6 text-2xl font-semibold tracking-tight text-gray-900 dark:text-white sm:text-2xl">{t("features.title_story_down")}</h2>
		<p class="text-base leading-8 my-5">{t("features.tiktok_story_text1")} <a href="/tiktok-story-downloader" class="group">{t("features.title_story_link")}</a> {t("features.tiktok_story_text2")}</p>
		<p class="text-base leading-8 my-5">{t("features.tiktok_story_text3")} <a href="/tiktok-story-downloader" class="group">{t("features.title_story_downloader")}</a> {t("features.tiktok_story_text4")}</p>
     </div>
     </div>
